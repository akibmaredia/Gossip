namespace Library

module NetworkNode =

    let start system =
        ()
    
    let getNeighbours pid algorithm =
        List.empty

    let getRumourCount pid =
        0

    let sendRumour randomPid =
        ()

    let getSWtuple pid =
        (0, 0)

    let updateSelf pid (s, w) =
        ()

    let sendSWtuple pid (s, w) =
        ()
